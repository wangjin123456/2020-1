server:
    port: 9100
eureka:
   instance:
     hostname: 127.0.0.1
   client:
     service-url:
       defaultZone: http://${eureka.instance.hostname}:8100/eureka/
     register-with-eureka: true
     fetch-registry: true
spring:
  application:
    name: eureka-server